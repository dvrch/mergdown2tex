{
  "name": "mergdowntotex",
  "version": "3.0.1",
  "private": true,
  "description": "Merge & doc. export everything — embeds, Zotero/Pandoc citations, Mermaid, equations, cross-refs — into a unique .tex file. WASM engine.",
  "scripts": {
    "transpile": "node scripts/transpile.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
